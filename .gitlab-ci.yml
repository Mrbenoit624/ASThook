image: python:latest

pages:
  script:
    - pip install -r requirement.txt
    - cd docs
    - pip install -r requirement.txt
    - make html
    - mv build/html ../public
  artifacts:
    paths:
      - public
  only:
    - develop
    - master
