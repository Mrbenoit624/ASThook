image: "python:3.7"

pages:
  stage: deploy
  script:
    - pip3 install -r requirement.txt
    - cd docs
    - pip3 install -r requirement.txt
    - make html
    - mv build/html ../public
  artifacts:
    paths:
      - public
  only:
    - develop
    - master
