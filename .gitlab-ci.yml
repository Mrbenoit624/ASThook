image: python:latest

pages:
  script:
    - pip3 install -r requirement.txt
    - cd docs
    - pip3 install -r requirement.txt
    - make html
    - mv build/html ../public
  artifacts:
    paths:
      - public
  only:
    - develop
    - master
